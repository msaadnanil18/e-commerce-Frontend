'use client';

import Coupon from '@/components/admin/apps/coupon';
import { FC } from 'react';

const Coupons: FC = () => {
  return (
    <div>
      <Coupon />
    </div>
  );
};

export default Coupons;
